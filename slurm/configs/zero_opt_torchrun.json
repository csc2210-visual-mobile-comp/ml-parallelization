{
  "train_batch_size": "auto",
  "micro_batch_size_per_gpu": 2,

  "fp16": {
    "enabled": true
  },

  "zero_optimization": {
    "stage": 2,
    "overlap_comm": true,
    "contiguous_gradients": true,
    "reduce_bucket_size": 2.5e7,
    "allgather_bucket_size": 2.5e7,
    "allgather_partitions": true,
    "reduce_scatter": true
  }
}